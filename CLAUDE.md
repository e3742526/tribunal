# Claude adapter instructions

Read and follow `AGENTS.md` first; it is the canonical single source of truth
for this repo and this file never overrides it.

Tribunal is a Git-independent, document-only Go CLI. Use `scripts/check.sh` for
the local gate. Do not add repository-writing review behavior or bypass host
validation for edit hunks.
